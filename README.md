# codecheck
a code reviewer web app that uses claude code to review GPU and other code 


This is a simple but beautiful web application that checking out repositories from Github I mean clones repository from GitHub then analyzes the code and gives a review of quote quality and improvement suggestions are to the person who is requesting it with this application needs to do it has a total of three fields The first field is a text field where the URL of the code repository on Github will be pasted if no protocol is mentioned then the value before the slash is the user or account and the value after the slash is actually the gitreos the second field is a ulldown menu and in the ulldown menu  We will be able to select some ROM each is represented by a text file and this text file on the file system will have the extension TRM ET TRMPT P like ERMPT The first line of this text file is displayed in the in the pull down field the third field is actually a text area that will be populated by the selected text or file from the ulldown menu Then there is a submit button that says evaluate now when the submit button is pushed there should be like a rogress bar then the the text in the text area is treated as as prompt and this prompt will be used with a clawed code to analyze the repository and to give recommendations  The recommendations will be display at the end and the user will be  They to have the recommendations file as an issue in their repository however this prompt will only be there if the application has been authenticated against Github to verify this we will need the Github CLI installed and authenticated against G 
